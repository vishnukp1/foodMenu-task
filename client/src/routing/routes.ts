
import { MenuPage } from "../pages";

import { homePath} from "./route.constant";

const routes = [
  {
    path: homePath,
    component: MenuPage, 
    exact: true,
  },
 
];

export default routes;

